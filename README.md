# new_kamal
<!-- ALTER TABLE `customer_records` CHANGE `type` `type` ENUM('service','sale','installment','repair','parts','others','active') CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT 'service'; -->

